apiVersion: v1
kind: Service
metadata:
  labels:
    run: udagram-frontend
  name: udagram-frontend
spec:
  ports:
    - port: 8100
      protocol: TCP
      targetPort: 80
  selector:
    run: udagram-frontend
